

<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Books Store</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
 <style>
  .card-img-top {
    width: 100%;
    height: 300px;
    object-fit: contain; /* keeps the full image visible like a book */
    background-color: #f8f8f8; /* light background like Amazon */
    padding: 10px;
    border-bottom: 1px solid #e0e0e0;
  }

  .card {
    border-radius: 8px;
    transition: transform 0.2s ease-in-out;
  }

  .card:hover {
    transform: scale(1.02);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
  }

  .card-title {
    font-size: 1.1rem;
    font-weight: bold;
  }

  .card-text {
    font-size: 0.9rem;
  }

  .card-footer {
    background-color: #fff;
    border-top: none;
  }
</style>

</head>

<body>
  <%- include('partials/header') %>

  <div class="container mt-5">
    <h1 class="mb-4">📚 Book Management System</h1>
    <a href="/add" class="btn btn-primary mb-4">➕ Add New Book</a>

    <% if (books.length > 0) { %>
      <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-4">
        <% books.forEach((book) => { %>
          <div class="col">
            <div class="card h-100 shadow-sm">
              <% if (book.coverImageUrl) { %>
                <img src="<%= book.coverImageUrl %>" class="card-img-top" alt="<%= book.title %>">
              <% } else { %>
                <img src="https://via.placeholder.com/300x250?text=No+Image" class="card-img-top" alt="No image">
              <% } %>

              <div class="card-body">
                <h5 class="card-title"><%= book.title %></h5>
                <p class="card-text">
                  <strong>Author:</strong> <%= book.author %><br>
                  <strong>Category:</strong> <%= book.category %><br>
                  <strong>Language:</strong> <%= book.language %><br>
                  <strong>Published:</strong> <%= new Date(book.pubDate).toLocaleDateString() %><br>
                  <strong>Price:</strong> ₹<%= book.price %>
                </p>
              </div>

              <div class="card-footer d-flex justify-content-between">
                <a href="/edit/<%= book._id %>" class="btn btn-warning btn-sm">Edit</a>
                <form action="/delete/<%= book._id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure?')">
                  <button class="btn btn-danger btn-sm">Delete</button>
                </form>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <p class="text-muted">No books found. Add one now!</p>
    <% } %>
  </div>

  <%- include('partials/footer') %>
</body>

</html>
